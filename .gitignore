build/
.cache/
*.o
*.a
*.so
*.dll
*.exe
*.out
CMakeUserPresets.json
compile_commands.json
.vscode/
.idea/

# Built book — sold as the typeset convenience version; build it from book/ if you prefer
book/*.pdf
# Derived from page 1 of that PDF, so equally a build artifact.
book/cover.png
book/cover-square.png
book/*.epub

# Byte-compiled Python, which the build tooling leaves behind.
__pycache__/

# Local tool settings. settings.local.json carried an absolute path
# with a username and the trail of a debugging session, in a public repo.
.claude/
